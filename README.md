# Soc-TP
 two SOAP-based web services: 
 a banking service (BanqueService)
 a second service (SecondService)
 
 The architecture includes a server-side component that publishes both services and a client-side component that interacts with both services.
